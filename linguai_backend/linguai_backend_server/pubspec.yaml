name: linguai_backend_server
description: Starting point for a Serverpod server.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  bcrypt: ^1.1.3
  crypto: ^3.0.6
  dart_jsonwebtoken: ^3.2.0
  http: ^1.5.0
  mailer: ^6.6.0
  serverpod: 3.4.12

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.12
  test: ^1.25.5

serverpod:
  scripts:
    # Starts the server and applies migrations
    start: dart bin/main.dart --apply-migrations

    # Build the Flutter web app and copy it to the server's web directory.
    # Windows uses xcopy since flutter's --output flag doesn't work there.
    # See: https://github.com/flutter/flutter/issues/157886
    flutter_build:
      windows: >-
        (if exist web\app rmdir /S /Q web\app)
        & cd /D ..\..
        && flutter build web --base-href /app/ --wasm
        && xcopy /E /I /Y build\web linguai_backend\linguai_backend_server\web\app
      posix: |
        cd ../..
        flutter build web --base-href /app/ --wasm --output linguai_backend/linguai_backend_server/web/app
