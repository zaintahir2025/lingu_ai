class: AiUsageRecord
table: ai_usage
fields:
  userId: int
  requestCount: int, default=0
  tokensUsed: int, default=0
  lastReset: DateTime
indexes:
  ai_usage_user_unique_idx:
    fields: userId
    unique: true
