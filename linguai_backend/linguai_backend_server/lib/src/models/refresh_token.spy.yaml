class: RefreshTokenRecord
table: refresh_token
fields:
  tokenHash: String
  userId: int
  device: String?
  expiresAt: DateTime
  createdAt: DateTime
indexes:
  refresh_token_hash_unique_idx:
    fields: tokenHash
    unique: true
  refresh_token_user_idx:
    fields: userId
