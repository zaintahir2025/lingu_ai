class: AdminAuditLogRecord
table: admin_audit_log
fields:
  actorUserId: int?
  action: String
  targetType: String
  targetId: String?
  details: String?
  createdAt: DateTime
indexes:
  admin_audit_created_idx:
    fields: createdAt
