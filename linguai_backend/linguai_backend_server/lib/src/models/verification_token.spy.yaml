class: VerificationTokenRecord
table: verification_token
fields:
  email: String
  tokenHash: String
  expiresAt: DateTime
  createdAt: DateTime
indexes:
  verification_token_hash_unique_idx:
    fields: tokenHash
    unique: true
