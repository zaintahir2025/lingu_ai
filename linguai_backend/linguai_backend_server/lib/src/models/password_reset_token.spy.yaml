class: PasswordResetTokenRecord
table: password_reset_token
fields:
  email: String
  tokenHash: String
  expiresAt: DateTime
  createdAt: DateTime
indexes:
  password_reset_token_hash_unique_idx:
    fields: tokenHash
    unique: true
